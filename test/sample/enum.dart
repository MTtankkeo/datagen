/// Defines a test enumeration with three values, e.g. (a, b, and c.)
/// Each enum value is associated with a string key for testing purposes.
enum Enums {
  a("a"),
  b("b"),
  c("c");

  final String key;
  const Enums(this.key);
}
